server:
  port: 9090

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/jpatest
    username: jpatest
    password: qwer1234!
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        show_sql: true
        format_sql: true